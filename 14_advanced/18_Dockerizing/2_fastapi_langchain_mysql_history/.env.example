# Copy this file to .env and fill in a real key - never commit the real .env.
# docker-compose.yml reads this via `env_file: .env` on the api service.
# MySQL credentials are set directly in docker-compose.yml's `environment:`
# block instead (demo-only plaintext, not secrets worth a .env entry here).
OPENAI_API_KEY=sk-your-key-here
