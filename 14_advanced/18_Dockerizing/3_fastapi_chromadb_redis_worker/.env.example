# Copy this file to .env and fill in a real key - never commit the real .env.
# Only the worker container uses this (see docker-compose.yml) - the api
# container never calls OpenAI directly.
OPENAI_API_KEY=sk-your-key-here
